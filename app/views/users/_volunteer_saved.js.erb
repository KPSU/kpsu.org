$("#dashboard-content").append("<div id='saved' class='invisible' title='Volunteer Hours Logged'><p>Your vounteer hours have been logged. To enter more, please refresh the page or click on volunteer hours link again.<br/><br/><span class='standard'>*click dismiss to continue using your dashboard.</span></p></div>");
$("#saved").dialog({
	modal:true,
	buttons: { "Dismiss": function() { $(this).dialog("close"); } }
});
$("input[type='submit']").attr("disabled", true).val("Saved!");
$("#dashboard-content").dialog('open')