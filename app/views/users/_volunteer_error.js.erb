$("#dashboard-content").append("<div id='saved' class='invisible' title='There was a problem'><p>Your volunteer hours cannot be logged for the following reasons:<ul><% @vh.errors.full_messages.each do |msg| %><li><%= msg %></li><% end %></ul></p></div>");
$("#saved").dialog({
	modal:true,
	buttons: { "Make Changes": function() { $(this).dialog("close"); } }
});
$("#dashboard-content").dialog('open')