<%= form_for(@promo, :html => {:multipart => true}) do |f| %>
  <% if @promo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promo.errors.count, "error") %> prohibited this promo from being saved:</h2>

      <ul>
      <% @promo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= radio_button_tag(:category, 1) %>
    <%= label_tag(:category_1, "Promo") %>
    <%= radio_button_tag(:category, 2) %>
    <%= label_tag(:category_2, "PSA") %>
    <%= radio_button_tag(:category, 3) %>
    <%= label_tag(:category_3, "PSA-Timely") %>
  </div>
  <div class="field">
    <%= f.file_field :promomp3 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
